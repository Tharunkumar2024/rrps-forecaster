# Database connection string (async)
DATABASE_URL=postgresql+asyncpg://rrps_user:rrps_password@localhost:5432/rrps

# Database connection string (sync - for ML pipeline)
DATABASE_URL_SYNC=postgresql://rrps_user:rrps_password@localhost:5432/rrps

# Environment details
APP_ENV=development
DEBUG=True

# Server settings
HOST=0.0.0.0
PORT=8000

# Logging
LOG_LEVEL=INFO
